<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.irelandlight.mapper.GoodsDetailImgMapper">
<select id="selectGoodsDetailImg" resultType="com.irelandlight.vo.GoodsDetailImage">
    SELECT tb_goods_image.id,tb_goods_image.url
    FROM tb_goods_image
    WHERE #{id}=tb_goods_image.goods_id
</select>

</mapper>